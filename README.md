# react-news

A news reader built with React and [NewsAPI.org](https://newsapi.org/).

## Getting started

Install dependencies:

```shell
npm install -g yarn
yarn install
```

Run app:

```shell
yarn start
```
